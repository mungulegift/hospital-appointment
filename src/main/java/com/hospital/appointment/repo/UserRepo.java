package com.hospital.appointment.repo;

//import com.hospital.appointment.model.HospitalUser;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

//@Repository
//public interface UserRepo extends JpaRepository<HospitalUser, Long> {
////    HospitalUser findUsersByUserName(String userName);
////    HospitalUser deleteUsersByIdAndUserType(Long id, String userType);
//}
